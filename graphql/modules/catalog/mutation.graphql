
extend type Mutation {
    userAuth(
        email: String! @rules(apply: ["email", "required"]),
        password: String!
    ): Token!
    
    registerUser(
        first_name: String!,
        last_name: String!,
        email: String!,
        password: String!,
        password_confirmation: String!
    ): Token!
}